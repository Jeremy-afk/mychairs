<form method="post" action="{{ path('app_lounge_delete', {'id': lounge.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ lounge.id) }}">
    <button class="btn">Delete</button>
</form>
