{% extends 'base.html.twig' %}

{% block title %}Edit Lounge{% endblock %}

{% block body %}
    <h1>Edit Lounge</h1>

    {{ include('lounge/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('app_lounge_index') }}">back to list</a>

    {{ include('lounge/_delete_form.html.twig') }}
{% endblock %}
